services:
  app:
    build: .
    ports:
      - "8000:8000"
    volumes:
      - submissions:/app/data/submissions
      - logs:/app/data/logs
      - public:/app/data/public
    env_file:
      - .env
    restart: unless-stopped

volumes:
  submissions:
  logs:
  public:
